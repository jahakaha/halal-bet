root = "."

[build]
    cmd = "go build -o tmp/api ./cmd/api"
    bin = "tmp/api"
    include_ext = ["go"]
    exclude_dir = ["tmp", "vendor", ".git", "db"]
    delay = 500
    send_interrupt = true
    kill_delay = "1s"

[color]
    main = "yellow"

[log]
    time = true
